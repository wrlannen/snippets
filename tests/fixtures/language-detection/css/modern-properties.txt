.container {
    width: clamp(300px, 80vw, 1200px);
    margin: 0 auto;
}

.fluid-text {
    font-size: clamp(1rem, 2vw + 0.5rem, 2.5rem);
}

.aspect-box {
    aspect-ratio: 16 / 9;
    object-fit: cover;
}

.gap-example {
    display: flex;
    gap: 1rem;
}

.hidden-overflow {
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
}